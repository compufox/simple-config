# this is a comment
number = 4 

list = a, b, c, d   # inline comments are also supported

number-list=1, 2, 3, 4

mixed-list=1, b, 3, d

false-value=false

also_false = False

test_key = thisIsASecretKey

keyword = :test